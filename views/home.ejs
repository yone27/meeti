<div class="hero">
    <h1>Encuentra o crea un grupo para compartir lo que más te gusta</h1>
    <a href="/crear-cuenta"
        class="btn btn-amarillo">Obtener Cuenta</a>
</div>

<div class="buscador-inicio">
    <div class="contenedor">
        <h2>Busca un grupo en tu ciudad</h2>
        <form action="/busqueda" method="GET">
            <select name="categoria">
                <option value=""
                    selected>-- Todas --</option>
                <% categories.forEach(category => { %>
                <option value="<%=category.id%>"><%=category.nombre%></option>
                <%})%>
            </select>
            <input type="text"
                name="titulo"
                placeholder="Titulo Meeti">
            <input type="text"
                name="ciudad"
                placeholder="Ciudad">
            <select name="pais">
                <option value="" selected>-- Todos --</option>
                <option value="MEX">México</option>
                <option value="COL">Colombia</option>
                <option value="ARG">Argentina</option>
                <option value="ESP">España</option>
                <option value="USA">Estados Unidos</option>
                <option value="PER">Perú</option>
            </select>
            <input type="submit"
                value="Buscar"
                class="btn btn-rosa">
        </form>
    </div>
</div>

<div class="contenedor">
    <h2>Próximos Meeti's</h2>
    <div class="grid columnas-md-3">
        <% if(meetis.length) { %>
            <% meetis.forEach(meeti => { %>
            <div class="card">
                <% if(meeti.group.imagen){ %>    
                    <img src="/uploads/groups/<%= meeti.group.imagen %>">
                <% }else{ %>
                    <img src="/img/meeti.jpg">
                <% } %>
                <div class="card-texto">
                    <% 
                    moment.locale('es')
                    const fecha = `${meeti.fecha} ${meeti.hora}` 
                    %>
                    <p class="fecha"><%= moment(fecha).format('LLLL') %> Horas</p>
                    <a href="/meeti/<%= meeti.slug %>">
                        <h3><%= meeti.titulo %></h3>
                    </a>
                    <div class="info-autor">
                        <% if(meeti.user.imagen){ %>
                            <div class="imagen">
                                <img src="/uploads/profiles/<%= meeti.user.imagen %>">
                            </div>
                        <% }else{ %>
                            <div class="imagen"></div>
                        <% } %>
                        <div class="informacion">
                            <p>Organizado por:</p>
                            <p class="autor"><%= meeti.user.nombre %></p>
                        </div>
                    </div>
                </div>
            </div>
            <% }) %>
        <% } %>
    </div>
</div>

<main class="bg-gris categorias">
    <h2>Encuentra Grupos por Categoría</h2>

    <div class="contenedor">
        <ul class="listado-categorias  grid columnas-2 columnas-md-3">
            <% categories.forEach(category => { %>
            <li>
                <a href="/categoria/<%=category.slug%>">
                    <img src="/img/categoria_<%=category.id%>.jpg">
                    <p><%=category.nombre%></p>
                </a>
            </li>
            <%})%>
        </ul>
    </div>
</main>

<div class="meeti-bg">
    <div class="contenedor meeti grid columnas-md-2">
        <section>
            <h3>Crea un grupo</h3>
            <p>Comparte lo que te gusta o tu pasión con los demás, crea tu propio grupo hoy mismo sin costos</p>
        </section>
        <section>
            <h3>Únete a un grupo</h3>
            <p>Conoce personas con tus mismos gustos y pasiones, únete a un grupo ya existente y recibe notificaciones
                sobre próximos eventos</p>
        </section>
    </div>
</div>